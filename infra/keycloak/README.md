# Keycloak

Este directorio contiene los artefactos necesarios para inicializar el servidor de identidad.

- `realm-export.json`: definición inicial del realm **comercio** con los clientes para frontend y backend.
- Importa el archivo desde la consola de administración de Keycloak (`http://localhost:8080`) después de que el contenedor esté en ejecución.
